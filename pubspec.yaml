name: nav_with_go_router
description: Navigator Demo

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.4
  shared_preferences: ^2.0.15
  provider: ^6.0.1
  extended_masked_text: ^2.3.1
  go_router: ^3.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

